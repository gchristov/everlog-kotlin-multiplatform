<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:orientation="vertical">

    <androidx.cardview.widget.CardView
        android:layout_width="@dimen/card_collapsing_summary_size"
        android:layout_height="@dimen/card_collapsing_summary_size"
        app:cardBackgroundColor="@color/background_card_lighter"
        app:cardCornerRadius="@dimen/card_collapsing_summary_radius">

        <TextView
            android:id="@+id/titleLbl"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fontFamily="sans-serif-medium"
            android:gravity="center"
            android:textColor="@color/white_darker"
            android:textSize="19sp"
            tools:text="12" />
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/subtitleLbl"
        style="@style/Toolbar.HomeSubtitle.Expanded"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/activity_margin_half_half"
        tools:text="Exercises" />
</LinearLayout>