<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    tools:background="@color/background_base">

    <TextView
        style="@style/SettingsCategory"
        android:layout_marginBottom="@dimen/settings_category_title_margin_vertical"
        android:text="@string/integrations_title" />

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardBackgroundColor="@color/background_card"
        app:cardCornerRadius="@dimen/card_radius_default"
        app:cardElevation="@dimen/card_elevation_default">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:divider="@drawable/separator_line"
            android:orientation="vertical"
            android:showDividers="middle">

            <LinearLayout
                android:id="@+id/googleFitBtn"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="?android:attr/selectableItemBackground"
                android:clickable="true"
                android:gravity="center"
                android:orientation="horizontal"
                android:paddingLeft="@dimen/settings_card_content_margin_left"
                android:paddingTop="@dimen/margin_12"
                android:paddingRight="@dimen/settings_card_content_margin_left"
                android:paddingBottom="@dimen/margin_12">

                <ImageView
                    android:id="@+id/googleFitImg"
                    style="@style/SettingsIcon"
                    android:padding="@dimen/activity_margin_half_half_half"
                    app:tint="@android:color/transparent"
                    tools:src="@drawable/ic_integration_google_fit" />

                <TextView
                    android:id="@+id/googleFitTitle"
                    style="@style/SettingsTitle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    tools:text="Google Fit" />

                <TextView
                    android:id="@+id/googleFitField"
                    style="@style/SettingsValue"
                    android:layout_width="match_parent"
                    android:gravity="right"
                    tools:text="Connected" />
            </LinearLayout>
        </LinearLayout>
    </androidx.cardview.widget.CardView>
</LinearLayout>