<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <solid android:color="@color/background_card" />

    <!-- Hairline, just enough to separate it from cards of the same colour -->
    <stroke
        android:width="0.5dp"
        android:color="@color/background_card_lighter" />

    <corners android:radius="@dimen/card_radius_default" />
</shape>
